import * as d3 from "d3";
const $ = require("jquery");

import { jsonCopy } from "@/utils/copy";

const margin = {
    top: 0.01,
    left: 0.01,
    right: 0.01,
    bottom: 0.01,
};

export function overview2layout() {
    
}